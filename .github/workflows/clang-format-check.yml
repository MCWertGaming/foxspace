name: clang-format Check
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  formatting-check:
    name: Formatting Check
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run clang-format in the fox folder
      uses: mcwertgaming/clang-format-action@dockerFix
      with:
        clang-format-version: '12'
        check-path: 'fox'
    - name: Run clang-format in the tests folder
      uses: mcwertgaming/clang-format-action@docker-fix
      with:
        clang-format-version: '12'
        check-path: 'tests'
